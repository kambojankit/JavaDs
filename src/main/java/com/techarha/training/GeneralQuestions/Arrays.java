package com.techarha.training.GeneralQuestions;

import java.lang.reflect.Array;
import java.util.HashMap;
import java.util.Map;

public class Arrays {

    //TODO, given a circular sorted array, find the number of times the array is rotated.

    //TODO, given a circular sorted array, find search a number 'x' in array

    public static void main(String[] args) {
        int octa = 00;
        while(octa <=  024) {
            System.out.println(octa++);
        }


        String s = "\\d";
    }
}
